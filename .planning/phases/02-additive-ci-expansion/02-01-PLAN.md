---
phase: 02-additive-ci-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/services/feed_manager/test_feed_manager.py
  - backend/tests/unit/services/swipe_worker/test_swipe_worker.py
  - backend/tests/unit/api/test_health.py
  - backend/tests/unit/api/test_movies.py
  - backend/tests/unit/api/test_watchlist.py
autonomous: true
---

<objective>
Expand backend unit test coverage additively — new test files only, no edits to existing tests.

Purpose: Phase 1 covered build_matrix, recommender internals, hydrator, api/users, api/interactions. Uncovered high-value modules remain: FeedManager, swipe_worker, and several API routers. This plan closes those gaps without touching any existing file.
Output: Five new unit test files under `backend/tests/unit/` covering feed_manager, swipe_worker, and the api/{health, movies, watchlist} routers.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing test patterns to mirror (read, do NOT edit):
@backend/tests/unit/api/test_users.py
@backend/tests/unit/api/test_interactions.py
@backend/tests/unit/services/test_hydrator.py
@backend/tests/conftest.py

# Source under test:
@backend/src/movie_recommender/services/feed_manager
@backend/src/movie_recommender/services/swipe_worker
@backend/src/movie_recommender/api/v1/health.py
@backend/src/movie_recommender/api/v1/movies.py
@backend/src/movie_recommender/api/v1/watchlist.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for FeedManager and swipe_worker services</name>
  <files>backend/tests/unit/services/feed_manager/test_feed_manager.py, backend/tests/unit/services/swipe_worker/test_swipe_worker.py</files>
  <action>
    Read the feed_manager and swipe_worker service modules to identify pure/mockable functions and class methods. For each:
    - Mock Redis client (AsyncMock) and any DB/Recommender dependencies — do NOT hit real services.
    - FeedManager: test queue fill/drain, per-user key generation, empty-queue behavior, refill triggering when below threshold, deduplication against watched set.
    - swipe_worker: test preference derivation, batch processing, idempotency, error handling on malformed payloads.
    - Follow conventions from test_hydrator.py (AsyncMock + pytest-asyncio). Use `conftest.py` fixtures where they exist; do NOT modify conftest.py.
    - Create `__init__.py` in new subdirs if needed (empty file) so pytest discovers them.
  </action>
  <verify>cd backend && .venv/bin/python -m pytest tests/unit/services/feed_manager tests/unit/services/swipe_worker -v</verify>
  <done>Both new test files exist, all tests pass, zero edits to existing files.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for api/health, api/movies, api/watchlist routers</name>
  <files>backend/tests/unit/api/test_health.py, backend/tests/unit/api/test_movies.py, backend/tests/unit/api/test_watchlist.py</files>
  <action>
    Mirror the pattern in test_users.py and test_interactions.py: use FastAPI TestClient with `app.dependency_overrides` to stub DB sessions, auth (`verify_user`), and external clients.
    - test_health.py: GET /api/v1/health returns 200 with expected payload shape.
    - test_movies.py: GET movie-by-id happy path + 404, list endpoint pagination/filters as implemented. Mock CRUD functions at import site (`movie_recommender.api.v1.movies.<crud_fn>`).
    - test_watchlist.py: add/remove/list watchlist with auth override; verify ownership checks return 403 on mismatch.
    - Do NOT modify any existing test, router, or conftest.
  </action>
  <verify>cd backend && .venv/bin/python -m pytest tests/unit/api/test_health.py tests/unit/api/test_movies.py tests/unit/api/test_watchlist.py -v</verify>
  <done>All three new test files exist and pass. Existing test suite still passes unchanged.</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && make unit-test` passes (existing + new tests)
- [ ] `git diff --name-only` shows only new files, zero modifications to pre-existing files
- [ ] `make format-check` passes on new files
</verification>

<success_criteria>
- Both tasks completed
- 5 new unit test files added, all passing
- No edits to existing source, tests, conftest, Makefile, or workflows
- Total unit test count increases; no prior tests break
</success_criteria>

<output>
After completion, create `.planning/phases/02-additive-ci-expansion/02-01-SUMMARY.md`
</output>
