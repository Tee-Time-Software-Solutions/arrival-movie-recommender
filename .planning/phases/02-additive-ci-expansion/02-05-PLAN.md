---
phase: 02-additive-ci-expansion
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/coverage-report.yml
autonomous: true
---

<objective>
Add a backend coverage reporting CI workflow.

Purpose: Phase 2 roadmap calls for coverage reporting. The backend is the priority stack, so this plan ships backend-only coverage now. Frontend coverage is bundled into the deferred Plan 02-04 and will be added later if/when that plan is executed.
Output: One new workflow file `.github/workflows/coverage-report.yml` running `pytest --cov` against the backend and uploading the report as an artifact.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Reference existing workflow for style:
@.github/workflows/ci-backend.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create coverage-report.yml workflow (backend only)</name>
  <files>.github/workflows/coverage-report.yml</files>
  <action>
    New workflow file. Requirements:

    **Triggers:** `push` on main and `pull_request` on all branches. Paths: `backend/**` and the workflow file itself.

    **Job: `backend-coverage`** on `ubuntu-latest`, `working-directory: ./backend`. Steps:
    1. `actions/checkout@v4`
    2. `astral-sh/setup-uv@v3` with cache (mirror ci-backend.yml)
    3. `actions/setup-python@v5` with python 3.11
    4. `make install`
    5. Install coverage plugin in-CI only: `uv pip install pytest-cov` (do NOT edit pyproject.toml)
    6. Run: `.venv/bin/python -m pytest tests/unit/ --cov=src/movie_recommender --cov-report=xml --cov-report=term` (invoke pytest directly — Makefile is off-limits)
    7. Upload `backend/coverage.xml` via `actions/upload-artifact@v4` as `backend-coverage`

    **Important:**
    - Do NOT fail the job on coverage thresholds — reporting only for Phase 2.
    - Do NOT edit backend/Makefile, backend/pyproject.toml, or any existing workflow.
    - Do NOT include a frontend coverage job — that belongs to the deferred Plan 02-04.

    Validate YAML.
  </action>
  <verify>python -c "import yaml; yaml.safe_load(open('.github/workflows/coverage-report.yml'))" && echo ok</verify>
  <done>coverage-report.yml exists, valid YAML, backend coverage job runs pytest --cov and uploads artifact, no edits to any existing file.</done>
</task>

</tasks>

<verification>
- [ ] `.github/workflows/coverage-report.yml` is valid YAML
- [ ] `git diff .github/workflows/ci-backend.yml` is empty
- [ ] No changes to Makefile, pyproject.toml, or any source file
- [ ] Workflow contains only the backend-coverage job (frontend deferred to 02-04)
</verification>

<success_criteria>
- New workflow file created, additive only
- YAML validates
- Backend coverage runs and uploads artifact on PRs
- Zero edits to existing files
</success_criteria>

<output>
After completion, create `.planning/phases/02-additive-ci-expansion/02-05-SUMMARY.md`
</output>
