---
phase: 02-additive-ci-expansion
plan: 05
type: execute
wave: 2
depends_on: ["02-04"]
files_modified:
  - .github/workflows/ci-frontend.yml
  - .github/workflows/coverage-report.yml
autonomous: true
---

<objective>
Add the frontend CI workflow and a cross-stack coverage reporting workflow.

Purpose: Plan 02-04 installed Vitest and created the first frontend tests but they currently run only locally. This plan wires them into CI via a new `ci-frontend.yml`, then adds a separate `coverage-report.yml` that runs both backend and frontend coverage and uploads the reports as artifacts.
Output: Two new workflow files under `.github/workflows/`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Reference existing workflow for style:
@.github/workflows/ci-backend.yml

# Depends on Plan 02-04 outputs:
@.planning/phases/02-additive-ci-expansion/02-04-SUMMARY.md

# Frontend test setup from Plan 02-04:
@frontend/app/package.json
@frontend/app/vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ci-frontend.yml workflow</name>
  <files>.github/workflows/ci-frontend.yml</files>
  <action>
    New workflow file. Requirements:

    **Triggers:** `push` and `pull_request` on all branches, scoped to `frontend/**` and `.github/workflows/ci-frontend.yml`.

    **Jobs:**

    1. `lint` — ubuntu-latest, working-directory `./frontend/app`. Steps:
       - `actions/checkout@v4`
       - `actions/setup-node@v4` with node-version 20 and `cache: 'npm'`, `cache-dependency-path: frontend/app/package-lock.json`.
       - `npm ci`
       - `npm run lint`

    2. `test` — ubuntu-latest, `needs: lint`, same working-directory. Steps:
       - checkout, setup-node (same config), `npm ci`
       - `npm test`

    3. `build` — ubuntu-latest, `needs: lint`, same working-directory. Steps:
       - checkout, setup-node, `npm ci`, `npm run build`
       - Set any `VITE_*` env vars to dummy values required for the build to succeed (mirror what env_config uses, but with fake values — do NOT commit real secrets).

    Do NOT modify any existing workflow file. Validate YAML.
  </action>
  <verify>python -c "import yaml; yaml.safe_load(open('.github/workflows/ci-frontend.yml'))" && echo ok</verify>
  <done>ci-frontend.yml exists, valid YAML, runs lint/test/build against frontend/app, no edits to existing workflows.</done>
</task>

<task type="auto">
  <name>Task 2: Create coverage-report.yml workflow</name>
  <files>.github/workflows/coverage-report.yml</files>
  <action>
    New workflow that produces coverage reports for both backend and frontend and uploads them as artifacts. Requirements:

    **Triggers:** `push` on main and `pull_request` on all branches (so PRs get coverage). Paths: `backend/**`, `frontend/**`, and the workflow file itself.

    **Jobs:**

    1. `backend-coverage` — ubuntu-latest, working-directory `./backend`. Steps:
       - checkout, setup-uv, setup-python 3.11 (mirror ci-backend.yml)
       - `make install`
       - `.venv/bin/python -m pytest tests/unit/ --cov=src/movie_recommender --cov-report=xml --cov-report=term` (do NOT add a Makefile target; invoke pytest directly).
       - Install coverage plugin if not already present: `uv pip install pytest-cov` as a separate step BEFORE running pytest, so we don't need to edit pyproject.toml.
       - Upload `backend/coverage.xml` via `actions/upload-artifact@v4` as `backend-coverage`.

    2. `frontend-coverage` — ubuntu-latest, working-directory `./frontend/app`. Steps:
       - checkout, setup-node 20 with npm cache
       - `npm ci`
       - `npm run test:coverage`
       - Upload `frontend/app/coverage/` via `actions/upload-artifact@v4` as `frontend-coverage`.
       - Set dummy VITE_* env vars if needed.

    Both jobs run in parallel (no `needs:`). Do NOT fail the workflow on coverage thresholds — this is reporting only in Phase 2; threshold enforcement is out of scope.

    Do NOT edit backend/Makefile, backend/pyproject.toml, frontend/app/package.json, or any existing workflow. The `uv pip install pytest-cov` step is an in-CI install only.
  </action>
  <verify>python -c "import yaml; yaml.safe_load(open('.github/workflows/coverage-report.yml'))" && echo ok</verify>
  <done>coverage-report.yml exists, valid YAML, runs backend and frontend coverage in parallel, uploads artifacts, no edits to any existing file.</done>
</task>

</tasks>

<verification>
- [ ] Both new workflow files validate as YAML
- [ ] `git diff .github/workflows/ci-backend.yml` is empty
- [ ] No changes to Makefile, pyproject.toml, package.json, or any source
- [ ] Workflows reference `npm test` and `npm run test:coverage` scripts that exist in package.json (added by Plan 02-04)
</verification>

<success_criteria>
- Two new workflow files created
- Frontend lint/test/build runs in CI on frontend changes
- Backend + frontend coverage runs and uploads artifacts on PRs
- Zero edits to existing files
</success_criteria>

<output>
After completion, create `.planning/phases/02-additive-ci-expansion/02-05-SUMMARY.md`
</output>
