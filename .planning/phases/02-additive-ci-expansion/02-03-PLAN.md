---
phase: 02-additive-ci-expansion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci-backend-integration.yml
autonomous: true
---

<objective>
Add a new CI workflow that runs backend integration tests against Postgres and Redis service containers.

Purpose: Current `ci-backend.yml` only runs lint + unit tests. Integration tests are never exercised in CI. This plan adds a separate workflow (additive, no edits to existing yml) that spins up real Postgres 16 + Redis 7.2 services and runs the integration suite — including the new tests from Plan 02-02.
Output: One new workflow file `.github/workflows/ci-backend-integration.yml`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Existing workflow to mirror style from (read, do NOT edit):
@.github/workflows/ci-backend.yml

# Infra reference for service versions/config:
@deployment
@backend/src/movie_recommender/core
@backend/src/movie_recommender/database/engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ci-backend-integration.yml workflow</name>
  <files>.github/workflows/ci-backend-integration.yml</files>
  <action>
    Author a new GitHub Actions workflow file. Key requirements:

    **Triggers:** `push` and `pull_request` on all branches, scoped to paths `backend/**` and `.github/workflows/ci-backend-integration.yml`. Match ci-backend.yml's style.

    **Job: `integration-tests`** on `ubuntu-latest`, `working-directory: ./backend`.

    **Service containers:**
    - `postgres:16` — env POSTGRES_USER/PASSWORD/DB set to match local dev defaults, health check with `pg_isready`, port 5432.
    - `redis:7.2` — health check with `redis-cli ping`, port 6379.

    **Steps:**
    1. `actions/checkout@v4`
    2. `astral-sh/setup-uv@v3` with cache (mirror existing workflow)
    3. `actions/setup-python@v5` with python 3.11
    4. `make install`
    5. Run Alembic migrations: `.venv/bin/alembic upgrade head` with DATABASE_URL env pointing at the postgres service.
    6. Run integration tests: `.venv/bin/python -m pytest tests/integration/ -v -m integration` (relies on Plan 02-02 tests being marked). Also run unmarked existing integration tests (`tests/integration/test_full_pipeline.py test_online_recommender.py`) to avoid regressing Phase 1 coverage.
    7. Env vars for test step: `DATABASE_URL`, `REDIS_URL`, `ENVIRONMENT=dev`, plus any minimal stubs required by AppSettings (dummy TMDB/Firebase keys so settings loads — DO NOT fail because of missing optional creds).

    **Critical constraints:**
    - Do NOT add an integration-test target to backend/Makefile (Makefile is off-limits). Invoke pytest directly.
    - Do NOT edit ci-backend.yml.
    - If AppSettings requires env vars that aren't available in CI, set them to obviously-fake values in the `env:` block rather than editing settings code.

    Verify YAML syntax with `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci-backend-integration.yml'))"`.
  </action>
  <verify>
    python -c "import yaml; yaml.safe_load(open('.github/workflows/ci-backend-integration.yml'))" && echo "valid yaml"
    Optionally: `gh workflow view ci-backend-integration` once pushed (actual run happens post-merge).
  </verify>
  <done>New workflow file exists, is valid YAML, references postgres:16 + redis:7.2 services, runs pytest against tests/integration/, does not edit any existing workflow.</done>
</task>

</tasks>

<verification>
- [ ] `.github/workflows/ci-backend-integration.yml` is valid YAML
- [ ] `.github/workflows/ci-backend.yml` is byte-identical to pre-plan state (`git diff .github/workflows/ci-backend.yml` is empty)
- [ ] No changes to backend/Makefile
- [ ] Workflow references `postgres:16` and `redis:7.2` service containers
</verification>

<success_criteria>
- New workflow file created, additive only
- YAML validates
- Workflow definition runs integration tests against real services in CI
- Zero edits to existing workflows, Makefile, or source
</success_criteria>

<output>
After completion, create `.planning/phases/02-additive-ci-expansion/02-03-SUMMARY.md`
</output>
