---
phase: 02-additive-ci-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/integration/test_api_smoke.py
  - backend/tests/integration/test_feed_manager_redis.py
  - backend/tests/integration/conftest_integration.py
autonomous: true
---

<objective>
Add backend integration tests that exercise the API and FeedManager against real Postgres and Redis.

Purpose: Existing integration tests (test_full_pipeline, test_online_recommender) cover the ML pipeline but not the FastAPI app or Redis-backed feed system end-to-end. Close that gap additively so the new integration-test CI workflow (Plan 02-03) has meaningful targets.
Output: Two new integration test files plus a dedicated conftest helper (new file, NOT editing existing integration/conftest.py) that provisions real DB/Redis sessions from env vars.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing integration patterns (read, do NOT edit):
@backend/tests/integration/test_full_pipeline.py
@backend/tests/integration/test_online_recommender.py
@backend/tests/integration/conftest.py
@backend/tests/conftest.py

# Source under test:
@backend/src/movie_recommender/main.py
@backend/src/movie_recommender/api/v1
@backend/src/movie_recommender/services/feed_manager
@backend/src/movie_recommender/database/engine.py
@backend/src/movie_recommender/core
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration conftest helper + API smoke tests</name>
  <files>backend/tests/integration/conftest_integration.py, backend/tests/integration/test_api_smoke.py</files>
  <action>
    Create a NEW helper module `conftest_integration.py` (do NOT touch existing `conftest.py`) that exposes pytest fixtures:
    - `real_db_engine`: builds an async SQLAlchemy engine from `DATABASE_URL` env var (fall back to localhost Postgres used in docker-compose). Runs Alembic upgrade head at session start, drops schema at session end.
    - `real_redis`: builds a redis.asyncio client from `REDIS_URL` env var.
    - `api_client`: FastAPI TestClient (or httpx.AsyncClient) with real dependency graph, auth dependency overridden to return a fixed test user.
    - Gate all fixtures with `pytest.importorskip` + skip-on-connection-error so the file is a no-op when services aren't available (local dev without infra should not break).

    In `test_api_smoke.py`, import fixtures from `conftest_integration` and write tests:
    - GET /api/v1/health returns 200.
    - Full swipe flow: create interaction → read user interactions → verify row persisted in real Postgres.
    - Onboarding or movies list endpoint happy path against real DB-seeded fixtures.

    Mark the module with `pytestmark = pytest.mark.integration` so it can be selected/deselected.
  </action>
  <verify>
    With Postgres + Redis running (docker compose): `cd backend && DATABASE_URL=... REDIS_URL=... .venv/bin/python -m pytest tests/integration/test_api_smoke.py -v`
    Without services: same command skips cleanly with "integration services unavailable".
  </verify>
  <done>New file executes real HTTP requests against a real DB when services are up, skips gracefully otherwise, does not modify existing conftest or tests.</done>
</task>

<task type="auto">
  <name>Task 2: FeedManager integration test against real Redis</name>
  <files>backend/tests/integration/test_feed_manager_redis.py</files>
  <action>
    New test file that imports the `real_redis` fixture from `conftest_integration`. Exercise FeedManager end-to-end:
    - Push N candidate movies into a user queue.
    - Drain via the same public methods the API uses.
    - Verify TTLs, idempotent pushes, cross-user isolation, refill behavior when below threshold.
    - Clean up keys in a finalizer (use a unique key prefix like `test:feed:{uuid}:` so parallel runs don't collide).

    Gate with same importorskip / connection-skip logic. Add `pytestmark = pytest.mark.integration`.
  </action>
  <verify>cd backend && REDIS_URL=redis://localhost:6379/0 .venv/bin/python -m pytest tests/integration/test_feed_manager_redis.py -v</verify>
  <done>Test passes against a real Redis instance, cleans up its own keys, skips cleanly when Redis is unavailable.</done>
</task>

</tasks>

<verification>
- [ ] New files parse and import: `python -c "import ast; [ast.parse(open(f).read()) for f in ['backend/tests/integration/conftest_integration.py', 'backend/tests/integration/test_api_smoke.py', 'backend/tests/integration/test_feed_manager_redis.py']]"`
- [ ] `make format-check` passes on new files
- [ ] Running `pytest tests/integration/` with no services does not crash existing tests (new files skip)
- [ ] `git diff --name-only` shows only new files
</verification>

<success_criteria>
- Two new integration test files + one new conftest helper
- Tests pass against real services (validated locally via docker compose or in Plan 02-03 CI run)
- Zero edits to existing files
- Tests use `pytest.mark.integration` so they can be selected by the new CI workflow
</success_criteria>

<output>
After completion, create `.planning/phases/02-additive-ci-expansion/02-02-SUMMARY.md`
</output>
