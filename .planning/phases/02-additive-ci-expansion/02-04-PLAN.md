---
phase: 02-additive-ci-expansion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/package.json
  - frontend/app/package-lock.json
  - frontend/app/vitest.config.ts
  - frontend/app/src/test/setup.ts
  - frontend/app/src/lib/utils.test.ts
  - frontend/app/src/stores/authStore.test.ts
  - frontend/app/src/components/features/MovieCard/MovieCard.test.tsx
autonomous: true
---

<objective>
Bootstrap a frontend test suite from scratch using Vitest + React Testing Library and add initial coverage for utilities, a Zustand store, and one feature component.

Purpose: The frontend currently has zero tests and no test tooling. Without a test script, the upcoming frontend CI workflow (Plan 02-05) has nothing to run. This plan stands up the tooling and a first batch of tests so Plan 02-05 can wire them into CI.
Output: Vitest config + setup file, three initial test files, and a `test` script in `package.json`. No edits to existing source code beyond adding the test script and devDependencies in package.json (package.json is NOT on the no-edit list).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Frontend structure to understand:
@frontend/app/package.json
@frontend/app/vite.config.ts
@frontend/app/tsconfig.json
@frontend/app/src/lib
@frontend/app/src/stores
@frontend/app/src/components/features
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest + RTL, add config and setup file</name>
  <files>frontend/app/package.json, frontend/app/package-lock.json, frontend/app/vitest.config.ts, frontend/app/src/test/setup.ts</files>
  <action>
    From `frontend/app/`:
    1. Install devDependencies: `npm install -D vitest @vitest/coverage-v8 @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom`.
    2. Add a `test` script and `test:coverage` script to package.json (`"test": "vitest run"`, `"test:coverage": "vitest run --coverage"`). Do NOT change any existing scripts.
    3. Create `vitest.config.ts` at `frontend/app/vitest.config.ts` using `defineConfig` from vitest/config with: `test.environment = 'jsdom'`, `test.globals = true`, `test.setupFiles = ['./src/test/setup.ts']`, `test.css = true`. Mirror path aliases from vite.config.ts so `@/` imports resolve in tests.
    4. Create `src/test/setup.ts` that imports `@testing-library/jest-dom` and sets up any global mocks needed (e.g. `window.matchMedia` stub, `IntersectionObserver` stub — keep minimal, add only if a failing test demands it).

    Do NOT edit vite.config.ts, tsconfig.json, or any source file. If tsconfig is missing vitest globals types, add types via a new `src/test/vitest.d.ts` file rather than editing tsconfig.
  </action>
  <verify>cd frontend/app && npx vitest run --reporter=verbose (should run zero tests successfully, confirming tooling is wired)</verify>
  <done>Vitest runs with zero failures, config + setup files exist, package.json has test scripts, no existing file other than package.json/package-lock.json is modified.</done>
</task>

<task type="auto">
  <name>Task 2: Write initial tests — utility, store, component</name>
  <files>frontend/app/src/lib/utils.test.ts, frontend/app/src/stores/authStore.test.ts, frontend/app/src/components/features/MovieCard/MovieCard.test.tsx</files>
  <action>
    Read the source for each target first (`src/lib/utils.ts`, `src/stores/authStore.ts`, the MovieCard component) to understand their actual API — do not guess.

    1. **utils.test.ts**: unit tests for the `cn()` helper (or whichever utilities exist). Cover: merging conflicting tailwind classes, conditional class filtering, empty input.

    2. **authStore.test.ts**: test the Zustand store actions in isolation by importing the store and calling actions directly. Mock Firebase if the store imports it at module load — use `vi.mock('@/lib/firebase', ...)` at the top of the file. Cover: initial state, setUser, clear/logout, token handling.

    3. **MovieCard.test.tsx**: render the component with a minimal mock movie prop using RTL `render()`. Assertions: title renders, poster renders (or fallback), interactive elements have accessible names. Wrap in router/providers only if the component demands them.

    Place test files NEXT TO their source (co-located `.test.ts[x]`) following a common React convention. Do not modify the source files.
  </action>
  <verify>cd frontend/app && npx vitest run</verify>
  <done>All three test files pass, zero edits to source files, tests run via `npm test`.</done>
</task>

</tasks>

<verification>
- [ ] `cd frontend/app && npm test` exits 0
- [ ] `cd frontend/app && npm run build` still succeeds (tooling didn't break the build)
- [ ] `cd frontend/app && npm run lint` passes (or at least doesn't regress vs. baseline)
- [ ] `git diff --name-only` shows only: package.json, package-lock.json, new test files, new vitest.config.ts, new src/test/setup.ts
</verification>

<success_criteria>
- Vitest + RTL installed and wired
- `npm test` runs and passes 3+ test files
- No edits to existing source files, vite.config.ts, tsconfig, or ESLint config
- Plan 02-05 can now reference `npm test` and `npm run test:coverage` in CI
</success_criteria>

<output>
After completion, create `.planning/phases/02-additive-ci-expansion/02-04-SUMMARY.md`
</output>
