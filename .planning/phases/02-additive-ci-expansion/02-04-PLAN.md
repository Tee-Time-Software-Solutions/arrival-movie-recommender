---
phase: 02-additive-ci-expansion
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - frontend/app/package.json
  - frontend/app/package-lock.json
  - frontend/app/vitest.config.ts
  - frontend/app/src/test/setup.ts
  - frontend/app/src/lib/utils.test.ts
  - frontend/app/src/stores/authStore.test.ts
  - frontend/app/src/components/features/MovieCard/MovieCard.test.tsx
  - .github/workflows/ci-frontend.yml
autonomous: true
priority: deferred
---

> **⚠️ DEFERRED — DO NOT EXECUTE IN FIRST PASS**
>
> This plan is deprioritized relative to the backend work in Phase 2. Execute plans 02-01, 02-02, 02-03, and 02-05 first. Only pick this up if there is remaining time/capacity after the backend-focused plans land.
>
> When deferred, the acceptance of Phase 2 does NOT require this plan to be complete.

<objective>
Bootstrap a frontend test suite from scratch using Vitest + React Testing Library, add initial coverage for utilities/store/component, and wire the new test suite into a new frontend CI workflow.

Purpose: The frontend has zero tests and no test tooling. This is valuable but lower priority than expanding backend test coverage and backend CI. Bundled into a single deferred plan so the frontend work can be picked up (or skipped) as a single unit.
Output: Vitest config + setup file, three initial test files, `test`/`test:coverage` scripts in `package.json`, and a new `.github/workflows/ci-frontend.yml` that runs lint/test/build on frontend changes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Reference existing workflow for CI style:
@.github/workflows/ci-backend.yml

# Frontend structure:
@frontend/app/package.json
@frontend/app/vite.config.ts
@frontend/app/tsconfig.json
@frontend/app/src/lib
@frontend/app/src/stores
@frontend/app/src/components/features
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest + RTL, add config and setup file</name>
  <files>frontend/app/package.json, frontend/app/package-lock.json, frontend/app/vitest.config.ts, frontend/app/src/test/setup.ts</files>
  <action>
    From `frontend/app/`:
    1. Install devDependencies: `npm install -D vitest @vitest/coverage-v8 @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom`.
    2. Add `test` and `test:coverage` scripts to package.json (`"test": "vitest run"`, `"test:coverage": "vitest run --coverage"`). Do NOT change any existing scripts.
    3. Create `vitest.config.ts` at `frontend/app/vitest.config.ts` using `defineConfig` from vitest/config with: `test.environment = 'jsdom'`, `test.globals = true`, `test.setupFiles = ['./src/test/setup.ts']`, `test.css = true`. Mirror path aliases from vite.config.ts so `@/` imports resolve in tests.
    4. Create `src/test/setup.ts` that imports `@testing-library/jest-dom` and sets up any global mocks strictly required by failing tests (matchMedia, IntersectionObserver — only if needed).

    Do NOT edit vite.config.ts, tsconfig.json, or any source file. If tsconfig is missing vitest globals types, add types via a new `src/test/vitest.d.ts` file rather than editing tsconfig.
  </action>
  <verify>cd frontend/app && npx vitest run --reporter=verbose</verify>
  <done>Vitest runs with zero failures, config + setup files exist, package.json has test scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Write initial tests — utility, store, component</name>
  <files>frontend/app/src/lib/utils.test.ts, frontend/app/src/stores/authStore.test.ts, frontend/app/src/components/features/MovieCard/MovieCard.test.tsx</files>
  <action>
    Read the source for each target first (`src/lib/utils.ts`, `src/stores/authStore.ts`, the MovieCard component) to understand their actual API — do not guess.

    1. **utils.test.ts**: unit tests for the `cn()` helper (or whichever utilities exist). Cover: merging conflicting tailwind classes, conditional class filtering, empty input.
    2. **authStore.test.ts**: test Zustand store actions by importing the store directly. Mock Firebase with `vi.mock('@/lib/firebase', ...)` if imported at module load. Cover: initial state, setUser, clear/logout, token handling.
    3. **MovieCard.test.tsx**: render with a minimal mock movie prop using RTL `render()`. Assertions: title renders, poster renders (or fallback), interactive elements have accessible names. Wrap in router/providers only if the component demands them.

    Co-locate test files next to source (`.test.ts[x]` siblings). Do not modify source files.
  </action>
  <verify>cd frontend/app && npx vitest run</verify>
  <done>All three test files pass, zero edits to source files.</done>
</task>

<task type="auto">
  <name>Task 3: Create ci-frontend.yml workflow</name>
  <files>.github/workflows/ci-frontend.yml</files>
  <action>
    New workflow file.

    **Triggers:** `push` and `pull_request` on all branches, scoped to `frontend/**` and `.github/workflows/ci-frontend.yml`.

    **Jobs:**
    1. `lint` — ubuntu-latest, working-directory `./frontend/app`. Steps: checkout, `actions/setup-node@v4` with node 20 + `cache: 'npm'` + `cache-dependency-path: frontend/app/package-lock.json`, `npm ci`, `npm run lint`.
    2. `test` — `needs: lint`, same setup + `npm test` + `npm run test:coverage`. Upload `frontend/app/coverage/` via `actions/upload-artifact@v4` as `frontend-coverage`.
    3. `build` — `needs: lint`, same setup + `npm run build`. Set `VITE_*` env vars to dummy values so the build doesn't require real secrets.

    Do NOT modify any existing workflow. Validate YAML.
  </action>
  <verify>python -c "import yaml; yaml.safe_load(open('.github/workflows/ci-frontend.yml'))" && echo ok</verify>
  <done>ci-frontend.yml exists, valid YAML, runs lint/test(+coverage)/build, no edits to existing workflows.</done>
</task>

</tasks>

<verification>
- [ ] `cd frontend/app && npm test` exits 0
- [ ] `cd frontend/app && npm run build` still succeeds
- [ ] `cd frontend/app && npm run lint` does not regress vs. baseline
- [ ] `.github/workflows/ci-frontend.yml` is valid YAML
- [ ] `git diff --name-only` shows only: package.json, package-lock.json, new test files, vitest.config.ts, src/test/setup.ts, ci-frontend.yml
</verification>

<success_criteria>
- Vitest + RTL installed and wired
- 3+ test files passing via `npm test`
- ci-frontend.yml present and valid
- No edits to existing source, vite.config.ts, tsconfig, ESLint config, or existing workflows
</success_criteria>

<output>
After completion, create `.planning/phases/02-additive-ci-expansion/02-04-SUMMARY.md`
</output>
